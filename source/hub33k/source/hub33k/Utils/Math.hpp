#pragma once

// TODO (hub33k): use glm
// check raylib math

namespace hub33k {

  template <typename T> struct Vector2 {
    T x, y;
  };

  template <typename T> struct Vector3 {
    T x, y, z;
  };

} // namespace hub33k
